Voici un Read me qui explique comment utiliser les hooks :

Dans .git/hooks il y a des hooks d'exemple avec l'extension .sample.

Il suffit d'ajouter le hook (ici pre-commit) dans le dossier .git/hooks et il se lancera tout seul à chaque commit.


